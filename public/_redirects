# Netlify SPA fallback.
# The app routes on the client (BrowserRouter), so every path has to be served
# the same index.html and let React Router decide. Without this, a visitor who
# reloads on /privacy-policy or lands there from a link gets Netlify's 404
# instead of the page. 200 = rewrite, not a redirect: the URL stays intact.
/*  /index.html  200
