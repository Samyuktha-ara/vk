# Netlify routing.
# Every route is prerendered to its own index.html at build time
# (scripts/prerender.mjs), so /privacy-policy and /terms are served as real
# files. Anything else gets the prerendered 404 page with a genuine 404
# status, so search engines never index a missing URL as a live page.
/*  /404.html  404
